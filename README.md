# Run This command
pkg update

pkg upgrade

pkg install python

pip install requests

pip install mechanize

pip install bs4

pkg install git

pip install rich

rm -rf OLD

git clone --depth=1 https://github.com/ABIRxADRIYAN/OLD.git

cd OLD

git pull

python OLD.py
